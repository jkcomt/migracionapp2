# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: cl.copec.zui5sdapplogmig
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://vhohcs4dci.sap.copec.cl:44300/
          client: '400'
        app:
          name: ZUI5_APP_LOGMIG
          package: ZCOPEC_SD
          transport: S4DK908642
        exclude:
          - /test/
